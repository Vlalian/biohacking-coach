# Example Feature — Agent-config onboarding

Summary
-------

Provide a small, example feature that demonstrates the repository's local-markdown issue workflow and how an agent can pick up an AFK-ready issue.

Goals
-----

- Show where PRDs and issues live under `.scratch/`.
- Provide a small scoped PRD that a contributor or agent can implement.

Acceptance criteria
-------------------

- A PRD file exists at `.scratch/example-feature/PRD.md`.
- At least one issue file exists under `.scratch/example-feature/issues/` with a `Status:` line.

Notes
-----

This is an example file created by the repository setup skill. Delete or replace it when you add real work.
